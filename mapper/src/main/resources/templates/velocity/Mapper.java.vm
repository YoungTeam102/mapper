package ${package}.mapper;

import ${package}.entity.${className}Entity;
import org.apache.ibatis.annotations.Mapper;

/**
 * ${tableComment}
 * <p>
 * 
 * @author ${author}
 * @date ${datetime}
 * @version 1.0.0
 */
@Mapper
public interface ${className}Mapper extends BaseMapper<${pkType}, ${className}Entity> {
	
}
